import {pack as localPack} from "../../../../../data/pack";

const initialState = {
    pack: localPack
}

export const EditorReducer = (state: TEditorState = initialState, action: any) => {
    return state;
}